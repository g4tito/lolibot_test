let fetch = require('node-fetch')

let handler = async(m, { conn, args, usedPrefix, command }) => {
if (budy == `${prefix + command}`){
conn.sendMessage(from, 'Teest', text)
}

}

handler.fail = null
handler.limit = false

module.exports = handler
